/** Reserved `.test` origin baked into the e2e build as NEXT_PUBLIC_API_URL; never resolvable, always intercepted. */
export const E2E_API_URL = "http://api.tallyhawk.test";

/** Where the mocked checkout session sends the browser. */
export const STRIPE_CHECKOUT_URL = "https://checkout.stripe.test/session/cs_test_123";
